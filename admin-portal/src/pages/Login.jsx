import { useState } from 'react';
import { Navigate, useNavigate } from 'react-router-dom';
import { LockKeyhole, Mail } from 'lucide-react';
import { useAuth } from '../auth/AuthContext';
export default function Login() { const { admin, login } = useAuth(); const navigate = useNavigate(); const [form, setForm] = useState({ username: '', password: '', remember: true }); if (admin) return <Navigate to="/dashboard" replace/>; const submit = (event) => { event.preventDefault(); login(form.username || 'مدير تجريبي', form.remember); navigate('/dashboard'); }; return <main className="login-page"><form className="login-card" onSubmit={submit}><div className="login-mark">ع</div><span className="eyebrow">بوابة إدارة عزيزا — نسخة تجريبية</span><h1>تسجيل دخول الإدارة</h1><p>واجهة محاكاة فقط. سيتم استبدالها بتسجيل دخول آمن عبر الـBackend لاحقاً.</p><label>البريد الإلكتروني أو اسم المستخدم <span><Mail size={16}/><input required value={form.username} onChange={(e) => setForm({ ...form, username: e.target.value })} placeholder="admin@example.test"/></span></label><label>كلمة المرور <span><LockKeyhole size={16}/><input required type="password" value={form.password} onChange={(e) => setForm({ ...form, password: e.target.value })} placeholder="••••••••"/></span></label><label className="remember"><input type="checkbox" checked={form.remember} onChange={(e) => setForm({ ...form, remember: e.target.checked })}/> تذكّر دخولي على هذا الجهاز</label><button className="primary" type="submit">دخول تجريبي</button></form></main>; }
