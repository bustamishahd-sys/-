import { useEffect, useState } from 'react';
import { ClipboardList, CircleCheck, Clock3, XCircle, ChartNoAxesCombined } from 'lucide-react';
import { getOrders } from '../services/api/orders';
import StatCard from '../components/StatCard'; import StatusBadge from '../components/StatusBadge';
export default function Dashboard() { const [orders, setOrders] = useState([]); useEffect(() => { getOrders().then(setOrders); }, []); const total = orders.reduce((sum, order) => sum + order.total, 0); return <><div className="page-heading"><div><span className="eyebrow">بيانات تجريبية</span><h1>نظرة عامة على العمليات</h1><p>ملخص الطلبات التجريبية فقط — لا توجد بيانات حقيقية في هذه النسخة.</p></div></div><section className="stats"><StatCard label="إجمالي الطلبات" value={orders.length} hint="طلبات Demo" icon={ClipboardList}/><StatCard label="بانتظار التأكيد" value={orders.filter((o) => o.status === 'pending').length} hint="تحتاج متابعة" icon={Clock3} tone="amber"/><StatCard label="طلبات مكتملة" value={orders.filter((o) => o.status === 'delivered').length} hint="في بيانات Demo" icon={CircleCheck}/><StatCard label="طلبات ملغاة" value={orders.filter((o) => o.status === 'cancelled').length} hint="في بيانات Demo" icon={XCircle} tone="red"/></section><section className="dashboard-grid"><article className="panel chart-panel"><div className="panel-head"><div><h2>نشاط الطلبات التجريبي</h2><p>عرض بصري تمهيدي لربط بيانات API لاحقاً.</p></div><ChartNoAxesCombined/></div><div className="bars">{[52, 74, 43, 66, 89, 58, 72].map((height, index) => <div key={index}><i style={{ height: `${height}%` }}/><small>{['س','ح','ن','ث','ر','خ','ج'][index]}</small></div>)}</div></article><article className="panel status-panel"><h2>الطلبات حسب الحالة</h2>{['pending', 'confirmed', 'preparing', 'out_for_delivery', 'delivered', 'cancelled'].map((status) => <p key={status}><StatusBadge status={status}/><b>{orders.filter((order) => order.status === status).length}</b></p>)}</article></section><article className="panel"><div className="panel-head"><div><h2>أحدث الطلبات التجريبية</h2><p>يمكن ربط هذه القائمة بـ <code>getOrders()</code> الحقيقي لاحقاً.</p></div></div><div className="table-wrap"><table><thead><tr><th>رقم الطلب</th><th>العميل</th><th>الإجمالي</th><th>الحالة</th></tr></thead><tbody>{orders.map((order) => <tr key={order.id}><td>{order.id}</td><td>{order.customer}</td><td>{order.total.toFixed(2)} ₪</td><td><StatusBadge status={order.status}/></td></tr>)}</tbody></table></div><div className="sales-total">إجمالي قيمة بيانات Demo: <b>{total.toFixed(2)} ₪</b></div></article></>; }
